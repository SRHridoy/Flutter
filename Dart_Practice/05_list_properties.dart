void main(){
  var city = ['Dhaka','Rajshahi','Barishal','Bogura'];
  
  print(city.first);
  print(city.last);
  print(city.length);
  print(city.reversed);
  print(city.isEmpty);
  print(city.isNotEmpty);
  //print(city.single); work if has one element...
}